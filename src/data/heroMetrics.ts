import type { HeroMetric } from "@/types";

/**
 * Метрики первого экрана. Число считается вверх, суффикс статичен —
 * поэтому «30M+» разложено на value 30 и suffix "M+".
 */
/**
 * ⚠️ Длина слова здесь — вопрос вёрстки. «Реализованных» — 13 знаков, самое
 * длинное слово на первом экране, и оно шире своей колонки при исходном кегле:
 * на 320 px колонка 83 px, а слово при 11 px / tracking 0.18em занимает 111 px.
 * `break-words` в таком случае рвёт слово ПОСРЕДИ — «РЕАЛИЗОВАНН|ЫХ», и так
 * было на 320, 360 и 375 px, то есть на всех ходовых мобилах.
 *
 * Слово оставлено (решение владельца), а разошлись подпись и колонка в
 * `HeroMetrics.tsx`: ниже sm кегль и трекинг подписи уменьшены до размера,
 * при котором 13 знаков влезают целиком. Заводя более длинное слово —
 * перемерять на 320 px, здесь запас всего ~9 px.
 */
export const HERO_METRICS: readonly HeroMetric[] = [
  { value: 48, suffix: "", label: "часов до первого запуска" },
  { value: 400, suffix: "+", label: "реализованных проектов" },
  { value: 30, suffix: "M+", label: "охватов в кампаниях" },
];

/** Цели count-up отдельной константой: стабильная ссылка для зависимостей хука. */
export const HERO_METRIC_TARGETS: readonly number[] = HERO_METRICS.map((m) => m.value);
