/**
 * Ручки фона Hero. Всё, что стоит крутить, — здесь.
 *
 * MVP-версия спеки docs/SPEC-hero-truss.md: геометрия фермы Уоррена осталась,
 * физика (передача усилия по стержням, прогиб, опоры) выброшена. Подсветка —
 * простой radial distance от курсора.
 */
export const GRID = {
  /** Шаг решётки по X, px CSS. rowY считается как spacing * 0.866 (равносторонний треугольник). */
  spacing: { sm: 88, md: 96, lg: 104 },
  /** Потолок узлов: выше — шаг растягивается, чтобы 4K не платил за плотность. */
  maxNodes: 420,
  /** Каждый N-й ряд поясов — силовой: толще и ярче. Без иерархии ферма выглядит как graph-фон. */
  majorChordEvery: 3,

  /** Радиус «фонарика», px CSS. */
  loadRadius: 190,
  /** Инерция курсора, мс. 0 — жёсткая привязка, ощущается как указка. */
  tauPointerMs: 90,
  /** Смещение узла под курсором, px. 0 = только подсветка, как в MVP-постановке. */
  nudge: 0,

  /** Амбиентное «дыхание»: одна изгибная форма пролёта. Два sin() на кадр, не на узел. */
  ambientAmplitude: 2.4,
  ambientPeriodMs: 11000,

  /** Пороги нормированного напряжения 0..1; акцент включается с индекса accentFromTier. */
  tierThresholds: [0.08, 0.24, 0.4, 0.58, 0.76] as const,
  accentFromTier: 3,

  /** Альфа линий в покое. Ориентир — прежняя .bg-grid на 0.03. */
  idleAlpha: { web: 0.045, chord: 0.085, major: 0.13, node: 0.1 },

  /** Кадров в секунду в живом режиме. */
  fps: 30,
  /** Потолок devicePixelRatio: заливка растёт как dpr². */
  dprCap: 2,
  /** Выше этой линии акцент не зажигается: шапка на mix-blend-difference
   *  инвертирует оранжевый в голубой и «VE» начинает мигать. */
  accentBanAboveY: 220,
  /** ACTIVE только у верха страницы — тот же порог, что уже использует Nav. */
  scrollGatePx: 24,
  resizeDebounceMs: 150,
} as const;

/** Маска канвы: прозрачно под шапкой, плотно в пустой зоне, спад к заголовку и CTA. */
export const GRID_MASK =
  "linear-gradient(to bottom, transparent 0, #000 96px, #000 45%, rgba(0,0,0,0.18) 72%, transparent 100%)";
