/**
 * Палитра «Leica Racing» — финальная, зафиксирована 2026-08-11.
 *
 * ⚠️ **ШЕСТЬ мест, и они обязаны совпадать.** Значения дублируются потому, что
 * читать их нужно из шести разных сред. Три первых очевидны, три последних —
 * нет, и при смене палитры 2026-08-11 они нашлись только грепом по хексам:
 *
 * | где | зачем |
 * |---|---|
 * | этот файл | JS/GLSL: uniform'ы wireframe-сцены цвет строкой не возьмут |
 * | `tailwind.config.js` | классы `bg-bg`, `text-accent`, `border-accent` |
 * | `src/index.css` `:root` | куда Tailwind не достаёт: `-webkit-text-stroke`, `::selection` |
 * | `index.html` | `theme-color` — им браузер красит адресную строку на мобиле |
 * | `scripts/generate-og.mjs` | константы OG-карточки; satori не читает ни Tailwind, ни TS |
 * | `public/favicon.svg` | сам знак; источник для `favicon.ico` и `apple-touch-icon` |
 *
 * Проверка после правки: `grep -rniE "старый-хекс" .` по всему репозиторию,
 * а не только по `src/`.
 *
 * Импортировать этот файл в `tailwind.config.js` нельзя: конфиг — `.js`,
 * а `allowJs` в `tsconfig` выключен, и загрузчик Tailwind TS-модуль не разберёт.
 * Поэтому дубль остаётся, но с явной ссылкой в каждом из трёх мест.
 * Расхождение `--accent` ↔ `colors.accent` — исторически известная ловушка репозитория.
 */
export const PALETTE = {
  /** Фон страницы. Глубже прежнего #0D0D0D. */
  bg: "#050505",
  /** Поднятые плоскости: карточки, панель параметров. */
  surface: "#111113",
  /** Акцент. Единственный насыщенный цвет в системе. */
  accent: "#D90429",
  /** Приглушённый акцент: вторые штрихи, задняя стенка wireframe, hairline-акценты. */
  accentMuted: "#8D081E",
  /** Основной текст. */
  textMain: "#F5F5F7",
  /** Второстепенный текст. Не задан постановкой — оставлен прежним. */
  textMuted: "#888888",
} as const;
