# Отчёт: мульти-ревью и починка, 2026-08-06

Ветка `fix/multi-review-2026-08-06`. Ревью поверх аудита 2026-08-05
([BACKLOG.md](BACKLOG.md)) и релиза ленты кейсов ([REPORT-cases-rail.md](REPORT-cases-rail.md)) —
искали **не то, что уже записано**, а то, что появилось после и осталось незамеченным.

---

## 1. Метод

Восемь независимых линз по коду: корректность React, целостность данных,
доступность, CSS и вёрстка, сборка и тулинг, SEO и `<head>`, производительность
рантайма, безопасность и контент. Каждая линза получала на вход бэклог целиком —
включая таблицу опровергнутых находок — с запретом переоткрывать известное.

Каждая находка проходила два фильтра: состязательную проверку по коду
(«попробуй опровергнуть») и — там, где это возможно — **замер в браузере**
на живой сборке. Ниже в графе «доказательство» стоит либо цитата кода, либо число.

Ограничение метода зафиксировано отдельно, см. §5: часть поведения в панели
браузера принципиально непроверяема, и одна гипотеза едва не попала в отчёт
ложной именно из-за этого.

## 2. Сводка

| | |
|---|---|
| Находок всего | 47 от восьми линз + 3 от критика полноты + 8 своих |
| Подтверждено и **исправлено** | 38 |
| Подтверждено, оставлено с обоснованием | 8 |
| **Опровергнуто состязательной проверкой** | 20 |
| Опровергнуто по ходу собственной проверки | 2 |

Severity выживших: 4 high, 14 medium, 20 low.

Двадцать находок из сорока семи не пережили проверку — это ожидаемая доля,
ради неё состязательный шаг и нужен. Три находки — про **ловушку фокуса
модалки**, каждая по своей причине. Это не случайность: рукописная ловушка
ошибается ровно там, где состояние фокуса задаёт не она, а браузер (оболочка
диалога с `tabIndex=-1`, кнопка, которая сама себя погасила). Обычный
«Tab на последнем → первый» такие переходы не воспроизводит, поэтому
предыдущий замер их и не поймал.

Три находки — про **ловушку фокуса модалки**, каждая по своей причине. Это не
случайность: ловушка фокуса, написанная вручную, ошибается ровно там, где
состояние фокуса задаётся не ей, а браузером (оболочка диалога с `tabIndex=-1`,
кнопка, которая сама себя погасила). Обычный «Tab на последнем → первый»
такие переходы не воспроизводит, поэтому предыдущий замер их и не поймал.

---

## 3. Исправлено

### 3.1 high · Кейс или участник без медиа роняет **всю страницу**

`src/data/media.ts`, `src/components/ui/CaseCard.tsx`, `src/components/Team.tsx`

`caseMedia` и `teamPhotos` были объявлены как `Record<string, CaseMedia>`.
В `tsconfig.json` включён `strict`, но **не** включён `noUncheckedIndexedAccess`,
поэтому `caseMedia[study.slug]` типизировался как `CaseMedia`, а не
`CaseMedia | undefined` — компилятор молчал. Дальше `media.cover` бросает
`TypeError` в фазе рендера, error boundary в дереве нет ни одного
([main.tsx](../src/main.tsx) монтирует `<App/>` напрямую), и React 18
демонтирует корень целиком: белый экран вместо лендинга.

Ловушка не теоретическая: [ARCHITECTURE §9](ARCHITECTURE.md) прямо предписывает
порядок «сначала объект `CaseStudy` в `cases.ts`, медиа — отдельным шагом».
Между двумя правками сайт лежит. Показательно, что `CaseModal` от того же
промаха был защищён (`{study && media && (`), а `CaseCard` — нет.

**Фикс.** Тип стал честным (`CaseMedia | undefined`), и компилятор теперь требует
проверки на каждом обращении. Карточка рисует пунктирный плейсхолдер «уточняется»
вместо обложки — по той же конвенции, что и чип незаполненного стека:
пробел должен быть виден, а не проглочен. Модалка не блокирует прокрутку
страницы, когда рендерить нечего (иначе кейс без обложки замораживал бы
страницу без единого видимого диалога).

### 3.2 high · Ловушка фокуса модалки протекает вперёд по Tab

`src/components/ui/CaseModal.tsx`

Было:

```js
if (event.shiftKey && (active === first || !dialogRef.current?.contains(active))) { … }
else if (!event.shiftKey && active === last) { … }
```

Ветка `shift+Tab` умела распознать «фокус ушёл наружу диалога», прямая — нет.

**Замер.** Открыть кейс 02, поставить фокус на «Предыдущий кейс», нажать:
индекс становится 0, `hasPrev → false`, React ставит кнопке `disabled`,
браузер по спецификации снимает с неё фокус и отдаёт `<body>`.
Проверено: `activeInsideDialog: false`, `activeAfterClick: "BODY"`.
Из этого положения штатный Tab уводил фокус за оверлей — на шапку и секции,
закрытые `bg-bg/80 backdrop-blur-md`, при `aria-modal="true"`, который
продолжал утверждать обратное. То же на последнем кейсе кнопкой «Следующий».

`REPORT-cases-rail.md` §3 отмечает ловушку фокуса как проверенную
(«Tab на последнем → первый, Shift+Tab на первом → последний») — именно
этот путь замером не покрыли.

**Фикс.** Обе ветки ловят «фокус вне диалога» симметрично; плюс при смене кейса
фокус подбирается обратно в диалог, если он успел уйти.
После правки: `focusInsideDialog: true`, Tab из этого положения фокус не выпускает.

### 3.2-бис high · Первый же Shift+Tab после открытия выносит фокус на почту в футере

`src/components/ui/CaseModal.tsx`

Отдельная дыра в той же ловушке, с другой причиной. При открытии фокус ставится
на **оболочку** диалога — `motion.div` с `tabIndex={-1}`. Этот элемент намеренно
исключён из списка фокусируемых (`[tabindex]:not([tabindex="-1"])`), поэтому
`active === first` ложно; при этом `contains(active)` истинно, ведь элемент
содержит сам себя, — значит, и «фокус снаружи» не срабатывает. Обе ветки молчат,
Shift+Tab отрабатывает нативно и уходит **назад по документу**. Портал лежит
последним ребёнком `<body>`, поэтому «назад» — это конец страницы.

**Замер сразу после открытия:** `activeIsDialogItself: true`,
`dialogContainsActive: true`, `activeIsFirstNode: false` → ни одна ветка не
сработает; ближайшая цель нативного Shift+Tab — `A · hq@vibeengineering.ru`,
ссылка в футере под оверлеем.

**Фикс.** «Начало списка» теперь включает саму оболочку диалога.
После правки: `prevented: true`, `insideDialog: true`, фокус садится на последний
элемент диалога.

### 3.2-тер high · `Alt+←` листает кейс и одновременно уводит браузер со страницы

`src/components/ui/CaseModal.tsx`

Обработчик стрелок не смотрел на модификаторы и не звал `preventDefault`.
`Alt+←` в Windows-браузерах — «назад по истории». Пользователь получал оба
действия сразу: кейс переключался, и тут же страница уходила назад.

**Фикс.** Стрелка считается «нашей», только если не нажаты Alt/Ctrl/Meta/Shift
и событие пришло не из медиаэлемента; при обработке гасим дефолт. Проверено:
`Alt+ArrowRight` — заголовок кейса не меняется и дефолт не погашен (браузер
делает своё), обычный `ArrowRight` — меняется и дефолт погашен.

### 3.3 medium · Стрелки ленты теряют фокус в `<body>` на краю ленты

`src/components/ui/CaseRail.tsx`

Тот же механизм без модалки: долистав кнопкой «→» до конца, пользователь нажимает
элемент, который тут же становится `disabled`. Фокус падает в `<body>`, и следующий
Tab начинает обход **с самого верха страницы** — то есть с шапки, мимо всей ленты.

**Фикс.** Фокус переезжает на соседнюю стрелку — но только в окне 1.5 с после
нажатия кнопки. Условие обязательно с двух сторон: без него эффект утаскивал бы
фокус к секции кейсов сразу после загрузки (`atStart` истинно с первого рендера),
а с «липким» флагом «ленту вели кнопками» — воровал бы его на любом позднем
`resize`, меняющем границы. Первая редакция фикса как раз была с липким флагом;
поймано следующей линзой ревью. Проверено обоими концами: фокус после загрузки
остаётся на `<body>`, а на краю ленты переходит на «Предыдущие кейсы».

### 3.4 medium · Ролик кейса: 59 секунд в петле без механизма паузы

`src/components/ui/LazyVideo.tsx`

`controls={reduce}` — контролы показывались **только** при
`prefers-reduced-motion: reduce`. В остальных случаях ролик играл
`autoplay + loop` без единого способа остановиться.

**Замер.** `ffprobe`: `duration=59.000000`. WCAG 2.2.2 требует механизм
паузы для любого автоматического движения дольше 5 секунд. Это отдельный,
не описанный в бэклоге случай того же нарушения, что §18 (бегущая строка).

**Фикс.** `controls` стоят всегда. Селектор ловушки фокуса модалки уже содержал
`video[controls]` — то есть исходный замысел был именно такой. Проверено:
плеер попадает в трап как четвёртый фокусируемый элемент.

Побочно: под плеером стрелки должны перематывать кадр, а модалка перехватывала их
на `document` и переключала кейс. Теперь события из медиаэлемента не перехватываются
(проверено: заголовок кейса после `ArrowRight` на видео не меняется).

### 3.5 medium · Мета-кейс публикует о себе неверные цифры

`src/data/cases.ts`

Блок «Кстати, этот сайт тоже мы» печатал посетителю три метрики. Все три разошлись
с реальностью, а одна — противоречила замеру из этого же репозитория:

| Метрика | На сайте | Факт |
|---|---|---|
| JS в проде, gzip | 95 KB | **99.87 KB** |
| CSS, gzip | 4 KB | **5.21 KB** |
| медиа на первом экране | **0 KB** | **17.4 KB** |

Третья — не просто устаревшая. С переходом на ленту кейсов две первые обложки
грузятся `eager` (`EAGER_COUNT = 2` → `loading="eager"`), то есть уходят в сеть
при первой же загрузке документа независимо от прокрутки. Ровно эти 17.4 kB
зафиксированы в [REPORT-cases-rail.md §5](REPORT-cases-rail.md) и в
[AGENTS.md §4](../AGENTS.md). Секция, которая продаёт инженерную точность цифр,
публиковала «0 KB» при собственном измерении 17.4 kB — и целевая аудитория
лендинга открывает DevTools.

`BACKLOG §50` этот пункт **описывал неверно**: как будущий риск («при заметном
росте бандла синхронизировать вручную»), только про JS и CSS, severity low.

**Фикс.** Приведено к замеру: `100 KB / 5 KB / 17 KB`.
⚠️ Третья строка — вопрос копирайта, а не разработки, см. §4.1.

### 3.6 medium · Плейсхолдер «уточняется» уезжал в структурированные данные

`src/components/ui/CasesJsonLd.tsx`

`keywords: study.stack.join(", ")` — а в `stack` у трёх кейсов из четырёх стоит
`TBD` («уточняется»), намеренный видимый пробел. В машинно-читаемом `CreativeWork`
он превращался в настоящий keyword: `"Telegram-бот, Tinder, уточняется"`.

**Фикс.** Плейсхолдер отфильтрован, при пустом остатке поле не выводится вовсе.
Проверено на живой странице: `"Telegram-бот, Tinder"`, оба блока `ld+json` парсятся.

### 3.7 medium · Скринридер зачитывает карточку кейса как имя кнопки — 243 символа

`src/components/ui/CaseCard.tsx`

Вся карточка — один `<button>`, у него нет явного имени, поэтому accessible name
считается из содержимого. **Замер:** 243 символа —
`"01 / 04Бот2022М.ВидеоНайм разработчиков через Tinder и Telegram-бота…650Kохв…"`.
Пользователь скринридера слышит слипшуюся простыню вместо «открыть кейс».

**Фикс.** Явный `aria-label`: `«Открыть кейс: М.Видео — Найм разработчиков через
Tinder и Telegram-бота»`. Внутренности остаются в DOM — их читает виртуальный
курсор, метка кнопки от них больше не зависит.

Структурная половина проблемы осталась осознанно, см. §4.2.

### 3.8 medium · Канва зовёт `getBoundingClientRect` на каждое движение пальца

`src/hooks/useStructuralGrid.ts`

Комментарий хука обещает: «static — prefers-reduced-motion ИЛИ touch. Один кадр,
**без rAF и без pointer-листенеров**». Фактически слушатели навешивались
безусловно, и обработчик `pointermove` звал `host.getBoundingClientRect()`
до всякой проверки режима — то есть на любом тач-устройстве при каждом
касании и свайпе, ради результата, который `draw()` гарантированно выбрасывает
(`px = FAR` в статическом режиме).

**Замер:** 20 синтетических `pointermove` → 20 вызовов `getBoundingClientRect`.
После правки — 0.

### 3.9 low · React-предупреждение на каждый рендер обеих обложек

`src/components/ui/Picture.tsx`

React 18.3.1 не знает проп `fetchPriority` (типы `@types/react` его уже содержат,
рантайм — ещё нет) и печатает в консоль `[error] Warning: React does not recognize
the fetchPriority prop…` со стеком на 14 строк. Функционально всё работало:
атрибут в DOM попадал (проверено — `fetchpriority="low"` на обеих eager-обложках).
Вред — в шуме: ошибка уровня `error` при каждой загрузке маскирует настоящие.

**Фикс.** Атрибут отдаётся строчными буквами через spread — такие React пропускает
молча. Проверено: атрибут на месте, консоль чистая.

### 3.10 low · `uspHeading.title` не читается ни одним компонентом

`src/data/usp.ts`, `src/components/USP.tsx`

Заголовок секции был захардкожен в разметке тремя `<span>`, а `uspHeading.title`
лежал в данных мёртвым: правка данных не давала никакого эффекта — молчаливо,
без ошибки. Ровно та ловушка, против которой заведена вся конвенция
«контент в `src/data`».

**Фикс.** Заголовок идёт из данных тремя кусками (крайние контурные с раскрытием,
средний — акцентный разделитель). Проверено: на странице `«Инженер × Продюсер»`.

### 3.11 low · Пользовательские строки в `.tsx` вопреки объявленной конвенции

`CaseCard.tsx`, `CaseRail.tsx`, `CaseModal.tsx` → `src/data/cases.ts`

AGENTS.md §5 и ARCHITECTURE §5 утверждают: «с контент-релиза v2 исключений нет,
в компонентах не осталось ни одной пользовательской строки». Лента кейсов
завела шесть новых: «Смотреть кейс», подсказка «Листайте вбок…» и четыре
`aria-label` (метка для скринридера — тоже строка для пользователя).

**Фикс.** Все шесть в `caseUiLabels` в `data/cases.ts`.

### 3.11-бис medium · `rootMargin` ролика не работал внутри модалки

`src/components/ui/LazyVideo.tsx`, `src/components/ui/CaseModal.tsx`

У наблюдателя `root` был вьюпортом, а ролик лежит в собственном скролл-контейнере
модалки (`max-h-[80svh] overflow-y-auto`). `rootMargin: "200px 0px"` расширяет
**root**, то есть вьюпорт, — а видимая область контейнера им не растягивается.
Предзагрузка за 200 px до появления не работала: ролик начинал грузиться ровно
в момент попадания в кадр, то есть с задержкой ровно там, где её и хотели убрать.

**Фикс.** `LazyVideo` принимает необязательный `scrollRootRef`; модалка передаёт
свой скролл-контейнер. Вне модалки поведение прежнее (root = вьюпорт).
⚠️ Замером в панели браузера **не подтверждено** — IO там не работает вовсе, см. §5.
Правка рассуждением по спецификации IntersectionObserver, риск низкий:
худший случай — поведение остаётся прежним.

### 3.11-тер medium · `sizes` обложек пропускал диапазон 640–767 px

`src/components/ui/CaseCard.tsx`

Было `"(min-width: 768px) 26rem, 85vw"`, а карточка на `sm` — `24rem` (384 px).
В полосе 640–767 px браузер считал слот равным 85vw (544–652 px) и выбирал из
`srcSet` кандидат крупнее нужного. Достаётся это в первую очередь двум обложкам,
которые грузятся `eager`, то есть тому самому весу первого экрана из §3.5.

**Фикс.** Средний брейкпоинт добавлен:
`"(min-width: 768px) 26rem, (min-width: 640px) 24rem, 85vw"`.

### 3.12–3.13 low · Мёртвый код, который врёт

- `ResponsiveImage.aspect` — поле объявлено и задокументировано, `Picture` его
  не читает: установка молча не даёт эффекта. Снято.
- `caseDataGaps` — комментарий утверждал, что от списка зависит плашка
  «уточняется» в ленте. Не зависит: плашку рисует `TBD` внутри `stack`,
  а сам `caseDataGaps` не импортируется ни одним компонентом. Комментарий
  переписан честно, экспорт оставлен — это справочник для дозаполнения.

---

### 3.14 high · Один из трёх каналов связи вёл на страницу посторонней компании

`index.html`, `src/data/contact.ts`

`github.com/vibeengineering` стоял и в `sameAs` структурированных данных, и третьим
блоком контактов на странице — с заголовком в `text-[13vw]`, наравне с Telegram
и почтой.

**Проверено через GitHub API:** это организация с `name: "VibeEngineering.ai"`,
`blog: https://www.vibeengineering.ai/`, **0 публичных репозиториев**, создана
2025-04-09. Бюро — `vibeengineering.ru`, ООО «ВАЙБ ИНЖИНИРИНГ», а его собственный
код лежит на `github.com/txtbyvova-ui/vibeengineering`. Совпадает только имя.

Два разных ущерба. Посетитель, нажавший «GitHub», попадал на пустую страницу
чужого аккаунта. Краулер получал `sameAs` — а это утверждение **тождества
сущностей**: ООО «ВАЙБ ИНЖИНИРИНГ» и VibeEngineering.ai склеивались в один узел
графа знаний.

`BACKLOG §9` объявлял пункт закрытым с критерием «оба URL отвечают 200».
Критерий недостаточен: 200 доказывает, что адрес резолвится, а не что аккаунт
принадлежит компании — `t.me` отдаёт 200 на любой несуществующий юзернейм.

**Фикс.** URL убран из обоих мест. ⚠️ Это снимает видимый блок со страницы —
см. §4.5, решение за владельцем.

### 3.15 medium · Петля бегущей строки рвётся на широких мониторах

`src/components/Marquee.tsx`, `src/data/clients.ts`, `tailwind.config.js`

Список дублировался ровно один раз, а анимация гонит трек до `translateX(-50%)`.
Бесшовно это только если половина трека шире вьюпорта.

**Замер:** одна копия — **2325 px**. На 2560 в конце цикла справа оставалась
пустая полоса **235 px** между двумя hairline-бордерами, на 3440 — **1115 px**,
то есть треть ленты пуста, и это последние 13 из 28 секунд каждого цикла.
На ноутбуке разработчика (1440 и ниже) дефекта нет — поэтому его и не видели.

**Фикс.** Четыре копии вместо двух: половина трека — 4649 px, хватает и на 4K.
Длительность в `tailwind.config.js` увеличена вдвое синхронно (28 → 56 s), иначе
вдвое длинный путь за то же время читался бы как удвоенная скорость;
пиксельная скорость осталась ровно прежней — 83 px/с.
Проверено на 2560: при конечном кадре правый край трека уходит **на 2089 px
за пределы** секции, зазора нет.

### 3.16 medium · Подпись метрики Hero наезжала на соседнюю колонку

`src/components/ui/HeroMetrics.tsx`

`grid-cols-3` жёсткий на всех ширинах, а неразрывное «реализованных» при
`tracking-[0.18em]` занимает 112 px против 83 px колонки.

**Замер на 320 px:** текст второй метрики шёл до x=230, третья колонка начинается
с x=217, обе строки на одной базовой линии → **12.9 px прямого наложения глифов**
на первом экране. На 360 px — стык впритык (0.5 px), на 375 px от 16-px гэпа
оставалось 5.5.

Опровергнутая находка бэклога «переполнения на 320 px нет» здесь не применима:
она замерена 2026-08-05, до появления `HeroMetrics`, и проверяла метки Contact.

**Фикс.** `break-words` на подписи — слово переносится вместо того, чтобы лезть
на соседа. После правки на 320 px ни одна подпись не выходит за свою ячейку
(запас 5–14 px), между колонками 21 и 30 px; горизонтального скролла нет.

### 3.17 medium · Портреты основателей сжимались флексом

`src/components/ui/Picture.tsx`

`shrink-0` и `w-24 md:w-32` висели на `<img>`, а флекс-элементом был `<picture>`,
который `Picture` рисует вокруг — с дефолтным `flex-shrink: 1`.

**Замер:** первый портрет 94 px вместо 96, второй 96 — карточки основателей
расходились по размеру, и чем длиннее имя, тем сильнее.

**Фикс.** `display: contents` на `<picture>`: раскладкой занимается `<img>`,
и классы наконец достаются тому элементу, которому адресованы. Проверено
по всем пяти потребителям `Picture`: портреты 128/128 на десктопе и 96/96
на мобиле, обложки кейсов и мета-кейса держат 16:10.

### 3.18 medium · Лента кейсов глотала `Alt+←`, а стрелки не двигали фокус

`src/components/ui/CaseRail.tsx`

Правка из §3.2-тер в файл-близнец не поехала: обработчик на `<ul>` ловил стрелки
без проверки модификаторов и безусловно звал `preventDefault`, отбирая у браузера
навигацию по истории. Достижимо сразу за Esc — `Cases.close()` сам возвращает
фокус на карточку.

Вторая половина, от критика полноты: стрелки двигали **ленту**, но не фокус.
Сфокусированная карточка уезжала за кадр, и следующий Tab отматывал ленту назад
к ней.

**Фикс.** Стрелка двигает фокус на соседнюю карточку, прокрутку за ним подтягивает
`keepInView`; при модификаторах обработчик выходит до `preventDefault`.
Проверено: `ArrowRight` переводит фокус 0 → 1 и гасит дефолт, `Alt+ArrowLeft`
не делает ни того, ни другого.

### 3.19 medium · Модалка закрывалась, если выделение текста отпустили на оверлее

`src/components/ui/CaseModal.tsx`

`click` по спецификации диспатчится на **общего предка** целей нажатия
и отпускания. Пользователь, выделявший текст внутри кейса и отпустивший кнопку
на поле оверлея, получал `click` на оверлее — `stopPropagation` на диалоге
в этом случае вообще не на пути распространения. Кейс закрывался, выделение
и позиция прокрутки терялись. Тем же путём закрывался кейс при попытке
потянуть полосу прокрутки оверлея.

**Фикс.** Закрытие по паре `pointerdown`/`pointerup`, и только если **обе**
цели — сам оверлей. Проверено все четыре случая: протяжка из текста на оверлей
не закрывает, чистый клик по оверлею закрывает, клик внутри диалога не закрывает,
Esc закрывает и возвращает прокрутку.

### 3.20 medium · Битый кэш шрифтов блокировал каждую последующую сборку

`scripts/generate-og.mjs`

Кэш проверялся только на существование файла, а писался неатомарно. Прерванная
сборка, кончившееся место или 200-ответ с HTML от корпоративного прокси
оставляли огрызок — и `existsSync` дальше всегда возвращал true, сеть не
спрашивалась, а `satori` падал невнятным «Offset is outside the bounds of the
DataView». Лечится только удалением каталога, о котором в сообщении ни слова.

**Фикс.** Запись через временный файл с `rename`; при чтении — структурная
проверка sfnt, и битый кэш скрипт чинит сам. ⚠️ Проверки **одной сигнатуры
недостаточно**: файл, урезанный до 4 КБ, первые четыре байта проходит — поэтому
читаем директорию таблиц и требуем, чтобы файл дотягивал до конца самой дальней.
Воспроизведено и проверено на обоих сценариях порчи: и обрезанный файл,
и HTML вместо шрифта теперь чинятся автоматически.

Заодно кэш переехал из `node_modules/.cache` в `.cache` — `npm ci` сносит
`node_modules` целиком. Это не делает сборку в CI офлайн-способной, см. §4.6.

### 3.21 low · Мелочи, которые врут или лежат мёртвым грузом

- **`parentOrganization`** объявлял `Organization` и `LocalBusiness` родителем
  и дочерней компанией — вопреки комментарию рядом, который утверждал тождество.
  Стало одним узлом с массивом `@type`. Проверено: граф парсится, узла `#business`
  больше нет.
- **`preconnect` промахивался мимо хоста шрифтов.** Проверено запросом: woff2
  Clash Display едет с `cdn.fontshare.com`, для которого прогрева не было вовсе,
  а `crossorigin` стоял на `api.fontshare.com`, который отдаёт CSS обычным
  запросом. Режимы приведены в соответствие, недостающий хост добавлен.
- **`/favicon.ico` отдавал 404.** Объявлен был только SVG; клиенты без его
  поддержки идут на `/favicon.ico` по умолчанию. Генерируется в `prebuild`
  вместе с apple-touch-icon (однокадровый ICO с PNG внутри, 674 байта,
  без второй зависимости). Проверено `file`: валидный ICO 32×32.
- **`.poster.webp` — 7.8 kB мёртвого файла в git:** `<video poster>` принимает
  ровно один URL, и плеер всегда просит `.jpg`. Снят с генерации и из git.
- **`media.ts` разошёлся с файлом:** объявлено 960×1212, реально 960×1214
  (проверено `ffprobe`). Интринсики брались от оригинала, а не от производной.
- **`META_SIZES` завышал слот** мета-кейса: `44vw` там, где от md стоит
  `md:max-w-md` — константа 448 px. Браузер брал из srcSet кандидат на ступень
  крупнее.
- **`maxFileBytes` в RAG-конфиге — 90000**, а AGENTS.md и MEMORY утверждали
  65536. Документация была неверна **изначально**, не устарела: при 65536
  из индекса молча выпала бы `docs/SPEC-hero-truss.md` (68 КБ). Числа в самом
  конфиге тоже протухли — lock-файл сегодня 111 КБ, а не 95.
- **`.gitignore`** не закрывал `.claude/settings.local.json` и `.shots/`.
- **`optimize-media.mjs`** проверял наличие исходника внутри цикла: падение
  на N-й записи оставляло `public/media` наполовину пересобранным. Проверка
  стала предполётной, по всему списку разом.
- **Кнопка карточки кейса без медиа была мёртвой:** `aria-haspopup="dialog"`
  на месте, а клик не давал ничего — модалка не рендерилась без обложки.
  Теперь раскрытие от обложки не зависит: текст, метрики, стек и ссылка
  на месте, вместо картинки — тот же пунктирный плейсхолдер.
- **Лента перерисовывала карточки на каждое событие scroll:** индикатор
  прогресса — состояние, а `ref` и `onOpen` были инлайновыми стрелками, так что
  React отцеплял и прицеплял все ref'ы карточек. Набор обработчиков
  мемоизирован, на карточке `React.memo`.
- **Две пользовательские строки оставались в `.tsx`** — `aria-label="Клиенты"`
  и имя списка для JSON-LD, при том что конвенция объявлена соблюдённой
  без исключений. Переехали в `data/clients.ts` и `data/cases.ts`.

## 4. Подтверждено, но не исправлено

### 4.1 ✅ ЗАКРЫТО 2026-08-07 · Метрика мета-кейса

**Решение владельца — вариант 2:** метрика переформулирована в бюджет,
не привязанный к сборке — `≤200 KB · бюджет медиа выше сгиба` (реальный бюджет
из AGENTS.md §4). Фактический расход сегодня — 17 kB из 200, запас
двенадцатикратный.

Ценность решения в том, что такая формулировка **не протухает от правок медиа**,
а именно это с прежней и случилось: «0 KB медиа на первом экране» было верно
до ленты кейсов и стало прямо неверным после неё. Две первые метрики (JS и CSS)
остались привязанными к билду — их синхронизировать при заметном росте бандла.

### 4.2 `<button>` карточки содержит `<h3>`, `<p>` и `<div>` — невалидный HTML

Модель содержимого `<button>` — только phrasing content. **Замер:** 12+
запрещённых потомков в одной карточке. Практический вред (имя кнопки на
243 символа) снят явным `aria-label` в §3.7, но остаётся структурный: заголовок
кейса не работает как заголовок, карточка не попадает в список заголовков
скринридера.

Полный фикс — перестройка карточки: `<article>` с настоящим `<h3>` и компактная
кнопка/ссылка внутри. Это переделка вёрстки и поведения ленты (клик по всей
площади карточки — сознательный приём), а не багфикс. Заведено в бэклог.

### 4.3 low · Прокрутка разблокируется на 240 мс раньше, чем исчезает оверлей

`src/components/ui/CaseModal.tsx`

Снятие блокировки висит на cleanup эффекта по `open`, а `open` становится ложным
сразу, тогда как `AnimatePresence` доигрывает выход ещё 240 мс. В это окно
страница уже прокручивается, шапка уже вернулась на полную непрозрачность,
а клики ещё съедает невидимая подложка.

**Оставлено осознанно.** Напрашивающийся фикс — перенести разблокировку
в `onExitComplete` — меняет дешёвый дефект на дорогой: если анимация выхода
не доиграет (прерванный размонтаж, нулевая длительность при reduced-motion,
исключение по пути), страница останется зафиксированной навсегда, без единого
способа прокрутить её. 240 мс проглоченных кликов — приемлемая цена за то,
что блокировка снимается безусловно.

### 4.5 ✅ ЗАКРЫТО 2026-08-07 · GitHub бюро

**Решение владельца: GitHub-аккаунта у бюро нет.** Блок и `sameAs` не
возвращаются, каналов связи два — Telegram и почта. Зафиксировано комментарием
в `src/data/contact.ts`, чтобы третий канал не завели по инерции.

### 4.6 ✅ ЗАКРЫТО 2026-08-07 · Сборка больше не зависит от зарубежных CDN

Решение владельца — закоммитить TTF и читать с диска. **Исполнено наполовину,
и вторая половина невозможна по лицензии.** Разбор ниже; полный провенанс —
в [assets/fonts/README.md](../assets/fonts/README.md).

**JetBrains Mono — закоммичен, редистрибуция разрешена явно.**
SIL Open Font License 1.1 прямо допускает распространение копий при условии,
что рядом едут текст лицензии и уведомление об авторских правах — `OFL.txt`
и `AUTHORS.txt` лежат в том же каталоге, неизменёнными. Файл взят из
официального релиза `JetBrains/JetBrainsMono` v2.304, вариант NL (без лигатур,
на 65 кБ легче), не модифицирован и не сабсетнут: правка задействовала бы
правило Reserved Font Name. Покрытие проверено разбором `cmap` — 122 кодпоинта
кириллицы, `◆`, `—`, `№`; все символы карточки покрыты посимвольно.

**Clash Display закоммитить НЕЛЬЗЯ.** Раздел «02. Limitations of usage»
ITF Free Font License:

> The Fonts may not — beyond the permitted copies and the uses defined herein —
> be distributed, duplicated, loaned, resold or licensed in any way … This
> includes the distribution of the Fonts by e-mail, on USB sticks, CD-ROMs, or
> other media, **uploading them in a public server** or making the fonts
> available on peer-to-peer networks.

Репозиторий `txtbyvova-ui/vibeengineering` — **публичный** (проверено через
GitHub API: `visibility: public`), то есть коммит TTF и есть «uploading them
in a public server». Тот же раздел отдельно запрещает без письменного согласия
ITF и селф-хостинг веб-шрифта («transmit the Font Software over the Internet
in font serving or for font replacement»).

Проверено и обратное — что мы делаем **законно**: §01 разрешает скачивание,
использование в коммерческих проектах и «reasonable number of back-up copies»
(локальный кэш вне git), а также создание статических изображений — то есть
сама OG-карточка. Преамбула EULA прямо называет Fontshare API штатным путём
доставки шрифта на сайт лицензиата: именно так его грузит `index.html`.

**Как цель достигнута без нарушения.** Clash Display стал необязательным:
скрипт берёт его по API и кэширует, но при отказе CDN **не падает** — латинский
заголовок набирается JetBrains Mono, карточка собирается, в лог уходит
предупреждение. Проверено прогоном с недоступным Fontshare и пустым кэшем:
`EXIT=0`, карточка 1200×630, 46.3 kB, «заголовок JetBrains Mono». Обратный
случай тоже проверен: пропажа вендоренного шрифта роняет сборку громко
и с указанием пути.

Итог по цели владельца: **деплой больше не может упасть из-за недоступности
зарубежного CDN.** Ценой того, что в этом сценарии карточка выходит не
в брендовой типографике — но выходит.

### 4.7 Мелкое, оставленное намеренно

- **`team/*-720.*` не выбирается ни при каком реальном DPR** — 129.6 kB
  закоммиченного медиа, которое никогда не отдаётся. Снять можно только правкой
  `WIDTHS.portrait` и прогоном `npm run media`, а тот требует исходного архива
  (`site media/` в `.gitignore`) и ffmpeg. Задача владельцу архива.
- **Токены `leading-*` перебиваются `line-height` из `md:text-*`** с 768 px,
  и `min-h-[2.6em]` карточки посчитан на мёртвом значении. Расхождение латентное:
  все четыре заголовка и так укладываются в две строки.
- **Мета-кейс на 768–1120 px** отдаёт картинке больше места, чем тексту.
  Оба напрашивающихся фикса **проверены замером и делают хуже** (`minmax(0,28rem)`
  даёт 155/387 вместо 340/202) — нужна переделка сетки, а не правка одного класса.

### 4.4 Документация разошлась с кодом

Не баг сайта, но ровно то, обо что спотыкается следующая сессия:

- `README.md` — карта папок без `CaseRail`/`CaseCard`/`CaseModal`/`CasesJsonLd`,
  без `data/structuralGrid.ts` и `hooks/usePrefersReducedMotion.ts`;
  Cases описан как «case grid», хотя это лента.
- `ARCHITECTURE.md` §3 — «Cases.tsx — сетка кейсов 2×N».
- `BACKLOG.md` §34 и `MEMORY.md` — «вёрстка Team завязана на `first`/`last`,
  третий участник сломает бордеры». В коде уже `md:odd:`/`md:even:` — пункт
  закрыт, но нигде не отмечен, и MEMORY продолжает предупреждать о несуществующем.

Приведено в порядок тем же коммитом, что и этот отчёт.

---

## 5. Ограничения проверки — важно для следующей сессии

**IntersectionObserver в панели браузера не доставляет колбэки вообще.**
Проверено собственным наблюдателем на трёх элементах, включая `h1` первого
экрана: ни одного вызова за 1.5 с при элементе целиком во вьюпорте.
Из этого следует, что в панели непроверяемы:

- ленивая загрузка картинок (`loading="lazy"` не срабатывает),
- `LazyVideo` — ролик никогда не получает `<source>`,
- `whileInView` Framer Motion (уже было в MEMORY.md),
- `useCountUp` — счёт не стартует, видны статические значения из разметки,
- `scrollBy({ behavior: "smooth" })` — не доезжает, потому что нет
  рендер-пайплайна.

Это едва не дало **ложную находку**: «ролик кейса не загружается никогда» —
симптом воспроизводился идеально и объяснялся кодом. Спасла проверка самого
инструмента: собственный `IntersectionObserver` на заведомо видимом `h1`
тоже молчал. Правило на будущее: прежде чем объявить сломанным то, что зависит
от IO, проверь IO на контрольном элементе.

Обход для таких проверок — headless Chrome через CDP (см. MEMORY.md) либо
ручная имитация: выставить `scrollLeft` напрямую и разослать событие `scroll`.

## 6. Опровергнуто по ходу

Записано, чтобы не искали заново.

| Гипотеза | Почему неверна |
|---|---|
| «Индикатор прогресса ленты стартует с неверной шириной» (замерено 18.3 % против верных 71.4 %) | Артефакт dev-сервера: в dev Tailwind приезжает инъекцией через JS, и первый `measure()` успевает отработать до применения стилей. В проде CSS — render-blocking `<link>`, к моменту монтирования вёрстка уже верная. Значение само исправляется на первом же `scroll`/`resize` |
| «`LazyVideo` никогда не грузит ролик» | Артефакт панели браузера, см. §5. В коде путь корректен, и замер веса в `REPORT-cases-rail.md` (1246 kB по клику) доказывает, что он работает |

## 7. Замеры до и после

```
                     до          после      Δ
dist/index.html    6.70 kB     7.23 kB    +0.53 kB   комментарии в <head>, favicon.ico
  gzip             2.16 kB     2.71 kB    +0.55 kB
dist/*.css        21.28 kB    21.55 kB    +0.27 kB   плейсхолдеры, break-words
  gzip             5.16 kB     5.24 kB    +0.08 kB
dist/*.js        303.58 kB   305.48 kB    +1.90 kB   подписи, guard'ы, фокус, memo
  gzip            99.46 kB   100.15 kB    +0.69 kB
модулей                418         418      —
+ dist/favicon.ico            674 байта   новый, закрывает 404
− public/media/…poster.webp  −7.8 kB      мёртвый файл снят с генерации и из git
```

Рост 0.6 % по JS — цена того, что страница перестала падать в белый экран,
перестала выпускать фокус из модалки тремя разными способами и перестала
закрывать кейс на попытке выделить в нём текст.

**Смоук в браузере:** 320, 375, 1280 и 2560 px. Консоль без ошибок
и предупреждений. Горизонтального скролла нет ни на одной ширине. Фокус после
загрузки не украден. Ловушка фокуса держит на обоих концах и из положения
«фокус на оболочке диалога». Оба блока JSON-LD парсятся, узел один, `sameAs`
только Telegram. Портреты основателей одного размера. Петля бегущей строки
на 2560 без зазора. `/favicon.ico`, `/og.png`, `/apple-touch-icon.png` — 200.
`npm run build` проходит, 418 модулей.

**Проверено отдельными прогонами скриптов:** порча кэша шрифтов обоими способами
(обрезание файла и HTML вместо TTF) — скрипт чинит сам и собирает карточку;
`favicon.ico` — валидный ICO 32×32 с PNG внутри по разбору контейнера.
