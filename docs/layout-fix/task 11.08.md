У нас проблемы с версткой: слишком много пустого пространства и наложения UI. Исправь следующие 4 критических пункта:

Картинка 1. СФЕРА НАЕЗЖАЕТ НА ПАНЕЛЬ LEVA:
В компоненте 3D-сцены (HeroWireframeCanvas / Scene) сдвинь геометрию или саму камеру левее. Например, задай объекту position={[-1.5, 0, 0]} (или подбери нужное значение), чтобы 3D-фигура визуально находилась в левой-центральной части экрана и не перекрывала правую панель с ползунками.

Картинки 2, 3, 4. ГИГАНТСКИЕ ПУСТЫЕ ПРОСТРАНСТВА (ДЫРЫ МЕЖДУ СЕКЦИЯМИ):
Пройдись по компонентам HeroWireframe.tsx, USP.tsx, Cases.tsx и Process.tsx.
- Удали все классы h-screen или min-h-screen, которые заставляют секции растягиваться на весь экран там, где контента мало.
- Уменьши гигантские отступы (замени py-32 или py-48 на адекватные py-16 или `py-24`).

- Верстка должна быть ПЛОТНОЙ. Секции должны идти друг за другом как единый поток, без белых/черных мертвых зон на несколько экранов. Немного воздуха не помешает, но не настолько, контент должен цеплять внимание сразу.

отменяем цвета из предыдущих экспериментов. Мы фиксируем финальную премиальную палитру "Leica Racing". 

Далее:
 ОБНОВИ ЦВЕТА (Tailwind / CSS переменные):
- --bg: #050505
- --surface: #111113
- --accent: #D90429
- --accent-muted: #8D081E
- --text-main: #F5F5F7
- Убедись, что wireframe-сфера в Hero, кнопки и метрики перекрасились в новый #D90429.