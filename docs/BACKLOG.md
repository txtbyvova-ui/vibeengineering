# Бэклог — Vibe Engineering Landing

Результат аудита 2026-08-05. Шесть независимых линз (a11y, performance, SEO, код,
тулинг, дизайн-система), каждая находка проходила состязательную проверку по коду:
72 находки → 63 подтверждено, 9 опровергнуто. Severity ниже — **после** корректировок
верификации, а не исходные оценки линз.

Часть пунктов дополнительно перемерена вручную в браузере на dev-сборке —
такие помечены **`[замерено]`** и имеют конкретные числа.

> Легенда effort: **S** — до часа, **M** — полдня, **L** — больше дня.
> Пункты с ❓ требуют решения владельца продукта, а не разработчика.

---

## Запланированные работы поверх бэклога

| Работа | Спецификация | Что тянет за собой |
|---|---|---|
| Переделка Hero: интерактивная ферма на Canvas 2D + строка метрик с count-up | [SPEC-hero-truss.md](SPEC-hero-truss.md) | ✅ **сделано** (`73b884c`, `b838af0`) |

⚠️ Постановка этой задачи пришла написанной **под другой стек** (Next.js app router, GSAP,
CSS-переменные `--bg`/`--text`/`--line`, классы `.mono`/`.body-text`/`grid-bg`/`grain`,
кастомный курсор `data-cursor`). Проверено: ничего из этого в репозитории нет, кроме
`.btn-fill`. Таблица соответствий — в §0 спецификации. Если бриф писался под другой
репозиторий — свериться до начала работ.

### Заведено мульти-ревью (ветка `fix/multi-review-2026-08-06`)

Полный разбор — [REPORT-multi-review-2026-08-06.md](REPORT-multi-review-2026-08-06.md).
Тринадцать находок того же ревью уже закрыты, они перечислены в блоке «Закрыто».

| # | Что | Severity | Комментарий |
|---|-----|----------|-------------|
| 55 | `<button>` карточки кейса содержит `<h3>`, `<p>` и `<div>` — невалидная модель содержимого | medium | **Замерено:** 12+ запрещённых потомков. Практический вред (имя кнопки на 243 символа) снят явным `aria-label`; остался структурный — заголовок кейса не работает как заголовок. Полный фикс — перестроить карточку в `<article>` с настоящим `h3` и компактной кнопкой внутри, то есть переделать поведение ленты (клик по всей площади — сознательный приём), а не поправить атрибут |
| 56 | Метрика мета-кейса «17 KB медиа на первом экране» — вопрос копирайта | low ❓ | Цифра теперь верная (было «0 KB» против замеренных 17.4 kB). Но прежняя формулировка была сильнее как аргумент. Варианты — в [отчёте §4.1](REPORT-multi-review-2026-08-06.md): оставить, перевести в бюджет «≤200 KB выше сгиба», или снять `eager` с обложек (последнее отменяет требование постановки) |
| 57 | Прокрутка разблокируется на 240 мс раньше, чем `AnimatePresence` снимает оверлей модалки | low | В это окно страница уже прокручивается и шапка вернулась, а клики ещё съедает невидимая подложка. **Оставлено осознанно:** перенос разблокировки в `onExitComplete` меняет дешёвый дефект на дорогой — недоигравшая анимация выхода оставила бы страницу зафиксированной навсегда. Разбор — [отчёт §4.3](REPORT-multi-review-2026-08-06.md) |
| 58 | GitHub бюро: блок контактов и `sameAs` сняты, потому что вели на чужой аккаунт | low ❓ | `github.com/vibeengineering` — организация «VibeEngineering.ai» (0 репозиториев, домен `.ai`), проверено через GitHub API. Вернуть можно одной строкой в [data/contact.ts](../src/data/contact.ts) и одной в `index.html`, **вписав настоящий аккаунт**. Если GitHub у бюро нет — так и оставить. Заодно критерий проверки `sameAs` в §9 надо заменить с «отвечает 200» на «профиль опубликован владельцем и ссылается обратно на vibeengineering.ru» |
| 59 | Сборка в CI жёстко зависит от `api.fontshare.com` и `fonts.googleapis.com` | medium ❓ | Кэш шрифтов переехал из `node_modules/.cache` (его сносит `npm ci`) в `.cache`, но чистому раннеру это не помогает. При отказе любого CDN `prebuild` делает `exit(1)` и деплой падает целиком из-за необязательной OG-картинки. Радикальное решение — закоммитить два TTF (~158 кБ) — требует проверки лицензий Fontshare и Google Fonts. Разбор — [отчёт §4.6](REPORT-multi-review-2026-08-06.md) |
| 60 | `team/*-720.*` не выбирается ни при каком реальном DPR — 129.6 kB мёртвого медиа в git | low | Снимается правкой `WIDTHS.portrait` в [scripts/optimize-media.mjs](../scripts/optimize-media.mjs) и прогоном `npm run media`, а тот требует исходного архива (`site media/` в `.gitignore`) и ffmpeg |
| 61 | Токены `leading-*` перебиваются `line-height` из `md:text-*` с 768 px | low | Проверено по собранному CSS и computed style. `min-h-[2.6em]` карточки посчитан на мёртвом значении. Расхождение латентное: все четыре заголовка и так в две строки |
| 62 | Мета-кейс на 768–~1120 px: трек `auto` отдаёт картинке больше места, чем тексту (340 против 202 на 768) | low | ⚠️ Оба напрашивающихся фикса **проверены замером и делают хуже**: `minmax(0,28rem)` даёт 155/387. Нужна переделка сетки, а не правка одного класса |

### Заведено лентой кейсов (ветка `feat/cases-rail`, 2026-08-06)

| # | Что | Severity | Комментарий |
|---|-----|----------|-------------|
| 51 | Категория `Концепция` добавлена сверх исходной пятёрки | low ❓ | В постановке было «сайт / бот / веб-приложение / AI / инсталляция». Альфа-Банк ни во что из этого не попадает: это стратегия и концепция, не продукт. Либо категория остаётся, либо кейс уходит с главной — решение владельца, см. отчёт |
| 52 | Пробелы в данных кейсов: `caseDataGaps` в [data/cases.ts](../src/data/cases.ts) | medium ❓ | Не хватает года у трёх кейсов и стека у трёх. В ленте на месте пропусков стоит `—` и пунктирный чип «уточняется» — намеренно видимый, чтобы не забылось |
| 53 | Решение и результат кейса видны только после клика | low | Текст живёт в модалке, то есть в DOM появляется по клику — краулер его не видит. Компенсировано `ItemList`/`CreativeWork` в [ui/CasesJsonLd.tsx](../src/components/ui/CasesJsonLd.tsx): содержимое отдаётся машинно-читаемым, без скрытого текста для роботов. Полностью снимется только пререндером (§8) |
| 54 | Обложка Альфа-Банка есть только до 960 px | low | Исходник `Alfa Bank Batte.webp` — 1192 px. В карточке (416 px) хватает, в модалке на retina — впритык. Нужен исходник крупнее |

### Заведено контент-релизом v2

| # | Что | Severity | Комментарий |
|---|-----|----------|-------------|
| 47 | Реквизиты ООО неполные: в футере только название, `legal.requisites` в [data/contact.ts](../src/data/contact.ts) — пустая строка | low ❓ | ИНН и ОГРН владелец не прислал. Поле готово: вписать строку вида «ИНН 7700000000 · ОГРН 1157700000000», разметка подхватит |
| 48 | Ролик М.Видео весит 1246 kB | low | Лениво, `preload="none"`, до вьюпорта — ноль байт. VP9 замеряли: на этом материале вышел тяжелее h264 (1917 против 1246 kB), поэтому второго `<source>` нет. Если понадобится урезать — либо crf выше 32, либо укоротить клип (это решение владельца, не разработки) |
| 49 | В ленте клиентов брендовую гарнитуру получают только `Kupikod`, `Poison Drop`, `VK Fest` | low | Прямое следствие §0: кириллические названия («Альфа-Банк», «М.Видео», «Сколково», «ВДНХ») рендерятся фоллбэком. До релиза латиницей было написано больше названий — теперь список приведён к копирайту владельца, и расхождение видно сильнее. Лечится только §0 |
| 50 | ✅ **ЗАКРЫТО** мульти-ревью. ⚠️ Пункт был сформулирован **неверно**: как будущий риск («при заметном росте бандла синхронизировать вручную») и только про JS/CSS. На деле расхождение уже произошло по всем трём метрикам, а третья («0 KB медиа на первом экране») противоречила замеру из этого же репозитория — 17.4 kB. Приведено к факту, остаток вопроса — в §56 | — | — |

---

## Закрыто

| Пункт | Как закрыт | Дата |
|---|---|---|
| §5 `og:image` | Генерируется на этапе сборки: `scripts/generate-og.mjs` (Satori + resvg), `npm run prebuild`. PNG пишется в `public/og.png` и закрыт `.gitignore` — политика «ноль ассетов в репозитории» не нарушена | 2026-08-05 |
| §6 соцмета неполная | Добавлены `og:site_name`, `og:locale`, `og:url`, `twitter:card`, `twitter:image` | 2026-08-05 |
| §7 `canonical` / `og:url` | Домен подтверждён владельцем: `https://vibeengineering.ru/` | 2026-08-05 |
| Дубль h2 «Инженер × Продюсер» | Заголовок остался в USP; Team получил собственный — «Кто строит» (по образцу Cases/Process) | 2026-08-05 |
| Смешанный алфавит в названии бренда | `M.ВИДЕО` (латинская `M` + кириллица) → `М.Видео` полностью кириллицей; то же в `cases.ts` | 2026-08-05 |
| §1 `prefers-reduced-motion` | Все три половины: CSS-блок в `@layer utilities`, `MotionConfig reducedMotion="user"` в `main.tsx`, и собственная JS-проверка в canvas-хуке (CSS и MotionConfig его не покрывают). Контент-релиз v2 добавил четвёртую — автоплей ролика в `LazyVideo` | 2026-08-05 |
| §21 focus-стиль на CTA Hero | `focus-visible:outline` акцентом. Остальные ссылки — по-прежнему на UA-кольце | 2026-08-05 |
| §34 бордеры Team на `first`/`last` | В коде уже `md:odd:`/`md:even:` — третий участник вёрстку не сломает. Пункт был закрыт раньше, но нигде не отмечен: и бэклог, и MEMORY продолжали предупреждать о несуществующем | 2026-08-06 |

### Закрыто мульти-ревью (ветка `fix/multi-review-2026-08-06`, 2026-08-06)

Разбор с доказательствами — [REPORT-multi-review-2026-08-06.md](REPORT-multi-review-2026-08-06.md).

| Пункт | Как закрыт |
|---|---|
| Кейс или участник без записи в `caseMedia`/`teamPhotos` ронял **всю страницу** белым экраном (`Record<string, T>` при выключенном `noUncheckedIndexedAccess` прячет `undefined`, error boundary нет) | Тип стал `| undefined` — компилятор требует проверки; карточка рисует пунктирный плейсхолдер, модалка не блокирует прокрутку, когда рендерить нечего |
| Ловушка фокуса модалки протекала **вперёд** по Tab: кнопка, ставшая `disabled`, роняет фокус в `<body>`, а прямая ветка проверяла только `active === last` | Обе ветки ловят «фокус вне диалога»; при смене кейса фокус подбирается обратно в диалог |
| Первый же **Shift+Tab** после открытия уводил фокус на почту в футере: фокус стоит на оболочке диалога (`tabIndex=-1`), она исключена из списка фокусируемых, но `contains` её признаёт — обе ветки молчали, и нативный Shift+Tab уходил назад по документу, а портал лежит последним ребёнком `<body>` | «Начало списка» включает саму оболочку диалога |
| `Alt+←` листал кейс **и** уводил браузер назад по истории: обработчик стрелок не смотрел модификаторы и не гасил дефолт | Стрелка считается «нашей» только без Alt/Ctrl/Meta/Shift и не из медиаэлемента; при обработке — `preventDefault` |
| `rootMargin` ролика не работал в модалке: `root` был вьюпортом, а ролик лежит в собственном скролл-контейнере — предзагрузка за 200 px не срабатывала | `LazyVideo` принимает `scrollRootRef`, модалка передаёт свой контейнер |
| `sizes` обложек пропускал диапазон 640–767 px — браузер брал кандидат крупнее нужного, причём у двух `eager`-обложек первого экрана | Добавлен средний брейкпоинт `(min-width: 640px) 24rem` |
| **Один из трёх каналов связи вёл на страницу посторонней компании:** `github.com/vibeengineering` — организация «VibeEngineering.ai», 0 репозиториев, домен `.ai` (проверено через GitHub API). Тот же URL в `sameAs` склеивал два юрлица в один узел графа знаний | URL убран из обоих мест; остаток вопроса — §58 |
| Петля бегущей строки рвалась на мониторах шире одной копии списка: 235 px пустоты на 2560, 1115 px на 3440 (замерено) | Четыре копии вместо двух (половина трека — 4649 px, хватает на 4K), длительность 28 → 56 s синхронно, чтобы скорость не изменилась |
| Подпись метрики Hero наезжала на соседнюю колонку — 12.9 px наложения глифов на 320 px, первый экран | `break-words` на подписи |
| Портреты основателей сжимались флексом: `shrink-0`/`w-24` висели на `<img>`, а флекс-элементом был `<picture>` — карточки расходились по размеру (94 против 96 px) | `display: contents` на `<picture>` в `Picture` |
| Лента кейсов глотала `Alt+←` (правка из CaseModal в файл-близнец не поехала), а стрелки двигали ленту, но не фокус — карточка уезжала за кадр | Стрелка двигает фокус на соседнюю карточку, прокрутку подтягивает `keepInView`; при модификаторах обработчик выходит до `preventDefault` |
| Модалка закрывалась, если выделение текста начато внутри диалога, а кнопка отпущена над оверлеем: `click` диспатчится на общего предка, и `stopPropagation` на диалоге не на пути | Закрытие по паре `pointerdown`/`pointerup`, и только если обе цели — сам оверлей |
| Битый кэш шрифтов блокировал **каждую** последующую сборку, включая офлайновую: проверялось только наличие файла, запись неатомарна | Запись через `rename`, структурная проверка sfnt при чтении, самолечение. ⚠️ Одной сигнатуры мало — обрезанный до 4 КБ файл её проходит |
| Кнопка карточки кейса без медиа была мёртвой: `aria-haspopup="dialog"` на месте, клик не давал ничего | Раскрытие больше не зависит от обложки — вместо картинки пунктирный плейсхолдер |
| `parentOrganization` объявлял одно бюро двумя организациями в иерархии, вопреки комментарию рядом | Один узел с массивом `@type: ["Organization", "LocalBusiness"]` |
| `preconnect` промахивался мимо хоста шрифтов: woff2 едет с `cdn.fontshare.com`, для которого прогрева не было вовсе, а `crossorigin` стоял на CSS-хосте | Режимы приведены в соответствие, недостающий хост добавлен |
| `/favicon.ico` отдавал 404 | Генерируется в `prebuild` (однокадровый ICO с PNG внутри, 674 байта, без новой зависимости) |
| `.poster.webp` — 7.8 kB мёртвого файла в git: `<video poster>` принимает один URL, плеер всегда просит `.jpg` | Снят с генерации и из git |
| `media.ts` объявлял 960×1212 при реальных 960×1214; `META_SIZES` завышал слот мета-кейса (`44vw` там, где `md:max-w-md` = 448 px) | Оба приведены к факту |
| `maxFileBytes` в RAG-конфиге — 90000, а AGENTS.md и MEMORY утверждали 65536; при 65536 из индекса молча выпала бы `SPEC-hero-truss.md` (68 КБ) | Документация исправлена по факту файла, числа в `_comment` перемерены |
| `.gitignore` не закрывал `.claude/settings.local.json` и `.shots/`; `optimize-media.mjs` проверял исходники внутри цикла и падал на середине прогона | Оба закрыты, проверка стала предполётной |
| Лента перерисовывала все карточки на каждое событие scroll и переприцепляла их ref'ы | Набор обработчиков мемоизирован, на карточке `React.memo` |
| Две пользовательские строки оставались в `.tsx` (`aria-label="Клиенты"`, имя списка для JSON-LD) при конвенции, объявленной соблюдённой без исключений | Переехали в `data/clients.ts` и `data/cases.ts` |
| Стрелки ленты теряли фокус в `<body>` на краю — следующий Tab начинал обход с верха страницы | Фокус переезжает на соседнюю стрелку, но только если ленту вели кнопками (иначе эффект крал бы фокус сразу после загрузки) |
| Ролик кейса (59 с, петля, автоплей) без механизма паузы — WCAG 2.2.2, отдельный от §18 случай | `controls` стоят всегда; стрелки под плеером больше не перехватываются модалкой |
| Плейсхолдер «уточняется» уезжал в JSON-LD настоящим `keywords` у трёх кейсов из четырёх | Отфильтрован, при пустом остатке поле не выводится |
| Accessible name кнопки карточки — 243 символа всей карточки | Явный `aria-label`: «Открыть кейс: клиент — заголовок». Структурная половина осталась, см. §55 |
| `pointermove` в статическом режиме канвы звал `getBoundingClientRect` на каждое касание (20 из 20, замерено) | Ранний выход по `isStatic()` — 0 вызовов |
| React 18.3.1 печатал ошибку про неизвестный проп `fetchPriority` на каждый рендер обеих обложек | Атрибут отдаётся строчными: в DOM как был, консоль чистая |
| `uspHeading.title` не читался ни одним компонентом — заголовок был захардкожен, правка данных молча не действовала | Заголовок идёт из данных тремя кусками |
| Шесть пользовательских строк ленты жили в `.tsx` вопреки конвенции, объявленной соблюдённой без исключений | `caseUiLabels` в `data/cases.ts` |
| Мёртвое поле `ResponsiveImage.aspect`; комментарий над `caseDataGaps` врал про связь с разметкой | Поле снято, комментарий переписан |

### Закрыто контент-релизом v2 (ветка `feat/content-v2`, 2026-08-05)

| Пункт | Как закрыт |
|---|---|
| §4 нет favicon | `public/favicon.svg` (монограмма VE) в git + `apple-touch-icon.png` 180×180, генерируется `scripts/generate-icons.mjs` в `prebuild` |
| §9 нет JSON-LD | Один `@graph` с `Organization` (+ `legalName`, фаундеры), `LocalBusiness` (адрес, каталог услуг) и `WebSite`; связаны через `@id`. В `sameAs` только профили, отвечающие 200 |
| §10 нет `robots.txt` / `sitemap.xml` | Оба в `public/`, отдаются 200, sitemap — валидный XML |
| §11 `<title>` на английском | Переписан на русский с гео: «Vibe Engineering — сайты, боты и веб-приложения на заказ, Москва»; в `description` добавлена Москва |
| §22 «Готовы?» не заголовок | Секция Contact перестроена: `h2` — «Есть задача — есть расчёт». `<footer>` **по-прежнему внутри `<main>`** — эта половина пункта открыта |
| §24 английские вставки без `lang` | `lang="en"` проставлен микро-лейблу Hero, ◆-надстрочникам и пунктам меню. Роли команды переведены на русский, вопрос снялся сам |
| §28 контент захардкожен в компоненте | В `.tsx` не осталось ни одной пользовательской строки: добавлены `data/{hero,usp,contact,nav,media}.ts` |
| Дубль М.Видео в ленте | Лента переписана по копирайту: `Kupikod · Альфа-Банк · М.Видео · Poison Drop · Сколково · VK Fest · ВДНХ`, латинский дубль снят |

⚠️ **§16 закрыт наполовину.** `.bg-grid` убран из-под Hero (`bg-bg` на секции),
но глобальная анимация `gridShift` осталась: это правка фона всей страницы,
её никто не санкционировал. Снять — одна строка в `index.css`.

⚠️ **Осталось решить по М.Видео.** Правка выполнена буквально по указанию («опечатка,
привести к М.Видео»), поэтому в ленте теперь **два** входа одного бренда:
`M.VIDEO` (латиница) и `М.Видео` (кириллица). Если дубль был не нужен —
удалить строку `{ name: "M.VIDEO" }` из [clients.ts](../src/data/clients.ts:6).

---

## P0 — брать первым

Дёшево, заметно, ничего не ломает.

### 0. Брендовые шрифты не поддерживают кириллицу `[замерено]` · high · L ❓

**Найдено 2026-08-05 при работе над OG-карточкой. По масштабу это крупнее всего
остального в бэклоге.**

Замеры (метод: сравнение ширины строки в `"Шрифт, monospace"` против чистого
`monospace` — совпадение означает, что шрифт в отрисовке не участвовал):

| Шрифт | Латиница | Кириллица |
|---|---|---|
| Clash Display (заголовки) | ✅ 357.5 ≠ 384.86 | ❌ 384.86 = 384.86 |
| Space Grotesk (весь корпус) | ✅ 575.4 ≠ 604.79 | ❌ 549.8 = 549.8 |
| JetBrains Mono (подписи) | ✅ | ✅ 420 ≠ 384.86 |

Подтверждено и на стороне CDN: `css2` для Space Grotesk объявляет только `latin`,
`latin-ext` и `vietnamese` — **кириллического сабсета не существует**. В TTF
Clash Display 600 — **0 кодпоинтов** в диапазоне U+0400–U+04FF (и нет даже `◆`).

**Следствие:** весь русский текст сайта — h1, абзацы, заголовки секций, имена
основателей, описания кейсов, цитаты — рендерится браузерным фоллбэком
`sans-serif` (Arial / Segoe UI), а не выбранной типографикой. Реальную гарнитуру
получают только латинские вкрапления: `VE`, `KUPIKOD`, `Selected Work`, `Founders`,
теги кейсов. То есть дизайн-система «Industrial Premium» не применяется примерно
к 95 % того, что читает русскоязычный посетитель — при том, что три семейства
исправно грузятся с двух сторонних CDN.

Это же объясняет замер §2: 3.88 px срезаемого выносного элемента посчитаны
по метрикам фоллбэка — то есть по тому, что реально видит пользователь.

**Fix — решение владельца, не разработчика.** Варианты:
1. Заменить display-шрифт на кириллический гротеск с похожим характером
   (Onest, Involve, Golos Text, TT Firs Neue) — сохраняет замысел, меняет лицо бренда.
2. Оставить Clash Display только для латиницы (`VE`, `VIBE ENGINEERING`,
   английские mono-подписи) и явно выбрать кириллическую пару для корпуса.
3. Ничего не менять и убрать Clash Display + Space Grotesk из загрузки —
   тогда честно экономим два render-blocking запроса к сторонним origin (§13),
   раз гарнитуры всё равно почти не работают.

**Чего делать нельзя** — оставить как есть, не приняв решения: сейчас проект платит
за шрифты трафиком и render-blocking-запросами, не получая их взамен.

> В OG-карточке это уже учтено: латинский заголовок набран Clash Display,
> весь русский текст и `◆` — JetBrains Mono, единственным шрифтом дизайн-системы
> с кириллическим покрытием.

### 1. Нет обработки `prefers-reduced-motion` `[замерено]` · high · M

**Замер:** 0 правил `prefers-reduced-motion` из 191 CSS-правила страницы; в момент
загрузки крутятся 3 бесконечные анимации (`gridShift`, `marquee`, `pulseDot`) плюс
`scroll-behavior: smooth` и 32 инстанса Framer Motion.

Пользователь с вестибулярными нарушениями получает движение, которое системной
настройкой отключить нельзя. Главный раздражитель — `marquee` 28 s с текстом 36–60 px
и smooth scroll; `gridShift` вносит мало (линии `rgba(136,136,136,0.03)` почти не видны),
reveal'ы играют по разу (`viewport={{ once: true }}`).

**Fix — обе половины обязательны:**

1. В [src/index.css](../src/index.css) — блок под `@layer utilities` (не голым правилом:
   утилиты Tailwind идут после пользовательских правил и перебьют его):
   `@media (prefers-reduced-motion: reduce)` → `animation-duration: .01ms !important`,
   `animation-iteration-count: 1 !important`, `transition-duration: .01ms !important`,
   `html { scroll-behavior: auto }`.
2. В [src/main.tsx](../src/main.tsx) обернуть `<App/>` в `<MotionConfig reducedMotion="user">`.
   **CSS сам по себе Framer Motion не гасит** — тот анимирует inline-стили через rAF/WAAPI.

### 2. `RevealText` обрезает выносные элементы букв `[замерено]` · high · S

**Замер** на слове «порталы.» в h1 (Clash Display 600, 72 px, `leading-[0.92]`):
высота маски **66.23 px**, нижняя граница ink-бокса глифов **70.12 px** →
**3.88 px описания буквы «р» срезано** `overflow-hidden`.

Затронуто везде, где `RevealText` стоит внутри заголовка с `leading < 1`:
[Hero.tsx:27](../src/components/Hero.tsx:27) `leading-[0.92]`,
[USP.tsx:35](../src/components/USP.tsx:35) `leading-[0.9]`,
[Contact.tsx:59](../src/components/Contact.tsx:59) `leading-[0.95]`, плюс заголовки
секций с `md:text-6xl` (line-height: 1) в Cases/Process/Team.

**Fix:** расширить окно клипа **симметрично** — при `leading < 1` срезается и верх
(буквы «й», «ё» — «Давай билдить», [Contact.tsx:65](../src/components/Contact.tsx:65)):
`pt-[0.15em] -mt-[0.15em] pb-[0.2em] -mb-[0.2em]` на маске **и одновременно** увеличить
стартовый сдвиг с `y: "110%"` — иначе текст будет виден в исходном положении.
Обязательна визуальная приёмка: точный запас зависит от метрик Clash Display.

### 3. `npm run lint` гарантированно падает · medium · S

[package.json:11](../package.json:11) объявляет `"lint": "eslint ."`, при этом eslint
нет ни в `dependencies`, ни в `devDependencies`, ни в `node_modules`, ни в
`package-lock.json`; конфига в репозитории тоже нет. Проверено запуском:
`'eslint' is not recognized`.

Прод не страдает — гейт `tsc --noEmit` в `build` работает. Страдает достоверность:
README и `package.json` обещают проверку, которой нет.

**Fix:** либо поставить eslint 9 с flat config (`eslint.config.js` +
`typescript-eslint` + `eslint-plugin-react-hooks`), либо убрать скрипт.
Половинчатого варианта нет.

### 4. Нет favicon · medium · S ✅ ЗАКРЫТО

`public/favicon.svg` (монограмма VE: `V` акцентом, `E` цветом `textMain`) лежит в git
как исходник; `apple-touch-icon.png` 180×180 собирается `scripts/generate-icons.mjs`
в `prebuild` и закрыт `.gitignore`. Веб-манифест не заводили — PWA-сценария нет.

---

## P1 — шеринг и индексация

### 5. Нет `og:image` · high · M ❓

Ссылка на сайт разворачивается в Telegram, Slack, VK и LinkedIn без картинки.
Это прямое следствие политики «в репозитории нет ни одного графического ассета»
(см. [ARCHITECTURE.md](ARCHITECTURE.md) §10) — то есть решение нужно принять, а не просто закодить.

**Fix:** сгенерировать OG-карточку 1200×630 в языке бренда (чёрный фон, Clash Display,
акцент), положить в `public/`, добавить `og:image`, `og:image:width/height`, `og:image:alt`.

### 6. Соцмета неполная · low · S

Нет `og:site_name`, `og:locale=ru_RU`, `og:url`, `twitter:card`.
`twitter:card="summary_large_image"` **бессмысленна до закрытия п.5** — без картинки
крупная карточка деградирует в ту же текстовую строку. Порядок: сначала п.5, потом
одним коммитом весь блок.

### 7. Нет `canonical` и `og:url` · medium · S ❓

Прод-домен нигде в репозитории не объявлен: есть только почта `hq@vibeengineering.ru`
и отсутствующий deploy-конфиг. `canonical` на несуществующий адрес хуже, чем его отсутствие.
**Сначала подтвердить хост (с `www` или без), потом добавлять.**

### 8. CSR без пререндера: в HTML отдаётся пустой `#root` · high · M

[index.html:40](../index.html:40) — `<div id="root"></div>`; в собранном
`dist/index.html` то же самое. Ни SSR, ни пререндера в зависимостях нет.

Тезис «соцботы не видят ни одного слова» **неверен** — `og:title`/`og:description`
лежат в статическом `<head>`, текстовое превью формируется. Реальный ущерб:
(а) Яндекс рендерит JS ограниченно и с задержкой, (б) при сбое загрузки бандла
(корпоративный прокси, блокировка домена) страница остаётся навсегда пустой —
ни одного контакта.

**Fix — дешёвый вариант предпочтительнее:** положить статический фолбэк прямо внутрь
`<div id="root">` (h1, оффер-абзац, 4 заголовка кейсов, контакты).
`ReactDOM.createRoot(...).render()` в [main.tsx:6](../src/main.tsx:6) полностью замещает
детей контейнера — дублей пользователь не увидит.
Тянуть `vite-prerender-plugin`/`react-snap` в проект без тестов и CI — избыточно.

### 9. Нет JSON-LD `Organization` · low · S ✅ ЗАКРЫТО

Один `@graph` в [index.html](../index.html): узел организации
(`@type: ["Organization", "LocalBusiness"]` — одна сущность, а не иерархия
из двух) с `legalName`, фаундерами, адресом и каталогом услуг, плюс `WebSite`,
связанный с ним через `@id`.

⚠️ **Критерий проверки `sameAs` был неверен.** Формулировка «оба отвечают 200»
доказывает только то, что адрес резолвится: `t.me` отдаёт 200 на любой
несуществующий юзернейм, а `github.com/vibeengineering` оказался посторонней
организацией «VibeEngineering.ai» (проверено через GitHub API 2026-08-06, ссылка
снята — см. §58). Правильный критерий: **профиль опубликован владельцем
и ссылается обратно на `vibeengineering.ru`.**

### 10. Нет `robots.txt` / `sitemap.xml` · low · S ✅ ЗАКРЫТО

Оба в `public/`. `robots.txt` — allow-all + ссылка на sitemap; `sitemap.xml` —
единственный `<loc>` с `lastmod`. Проверено на `vite preview`: 200 и валидный XML.
⚠️ `lastmod` жёстко вписан датой — при следующем релизе обновлять руками.

### 11. `<title>` полностью на английском · low · S ✅ ЗАКРЫТО

Стал «Vibe Engineering — сайты, боты и веб-приложения на заказ, Москва»;
в `description` добавлены гео и обещания 48 часов / 2 часов. `og:title`,
`og:description` и `twitter:*` приведены к тому же тексту.

---

## P2 — производительность

### 12. LCP полностью завязан на выполнение JS · high · M

Следствие п.8, усиленное `RevealText`: h1 в Hero собран из 6 `RevealText`, каждый
стартует с `y: 110%` внутри `overflow-hidden`, то есть визуально пуст;
[Hero.tsx:14](../src/components/Hero.tsx:14) и `:46` стартуют с `opacity: 0`
(элементы с `opacity: 0` Chrome вообще исключает из кандидатов LCP).

⚠️ Ранняя редакция [ARCHITECTURE.md](ARCHITECTURE.md) утверждала, что Hero использует
`animate`, а не `whileInView` — это верно только для микро-лейбла и абзаца; **сам h1 идёт
через `RevealText`/`whileInView`**. Документ исправлен.

**Fix:** для above-the-fold рендерить h1 без скрывающего `initial` (или сделать reveal
чистым CSS `@keyframes` со стартом по загрузке документа); `whileInView` оставить
секциям ниже сгиба. Реальный выигрыш по LCP даст только п.8.

### 13. Шрифты: цепочка HTML → CSS → woff2 с двух сторонних origin `[замерено]` · medium · M

**Замер:** 2 внешних запроса (`api.fontshare.com`, `fonts.googleapis.com`), оба —
render-blocking `<link rel="stylesheet">` в `<head>`. Объявлено **31** `@font-face`,
реально загружается **6** начертаний в 3 семействах.

Из этого следует, что **лишние веса в запросе (300/700) байт не стоят** — браузер качает
woff2 только для реально применённых начертаний. Первичная находка про «лишние сетевые
запросы» опровергнута.

Реальная цена — лишний RTT перед стартом загрузки woff2 и конкуренция за соединения
с 270 КБ JS. `preconnect` уже стоит ([index.html:22-24](../index.html:22)), `preload as=style`
для fontshare — тоже, так что худший сценарий частично закрыт.

**Fix:** self-host шрифтов в `public/fonts/` + `<link rel="preload" as="font" crossorigin>`.
Мерить по FCP/LCP до и после, а не по числу запросов.

### 14. Нет метрически совместимого fallback → CLS на главном заголовке · medium · M

`display=swap` + заголовок в `13vw`: подмена системного шрифта на Clash Display
двигает крупнейший блок страницы.

**Fix:** self-host + `preload` (чтобы загрузка началась до выполнения бандла) +
fallback-`@font-face` с `size-adjust`/`ascent-override`.
⚠️ `font-display: optional` **без preload сделает хуже** — шрифт не успеет к первому
paint и останется fallback навсегда.

### 15. Полный `framer-motion` ради простых reveal'ов · medium · M

~34 КБ gzip против ~18 КБ у связки `LazyMotion` + `domAnimation` + `m`.
Выигрыш реален, но вдвое меньше, чем показалось первичной находке.

⚠️ Замена `motion[as]` на `switch` в [RevealText.tsx:29](../src/components/ui/RevealText.tsx:29)
**сама по себе ничего не экономит** — полный набор фич тянется самим импортом `motion`,
это дизайн пакета. Нужен именно переход на `m` + `LazyMotion`.

### 16. `.bg-grid` перерисовывает вьюпорт каждый кадр · low · S

[index.css:62](../src/index.css:62) анимирует `background-position` на `fixed`-слое
во весь экран, бесконечно. Содержимое paint'а тривиально (два `linear-gradient`,
линии 1 px, alpha 0.03) — проблема в постоянстве, а не в стоимости кадра.

**Fix:** внутренний div, увеличенный на размер тайла, и `transform: translate3d`
вместо `background-position`. `will-change: transform` на всегда-анимируемом
полноэкранном слое стоит GPU-памяти — добавлять осознанно.

### 17. `vite.config.ts` без секции `build` · low · S

Нет `manualChunks` — весь код в одном бандле. Кэширование vendor-части при правках
контента не работает.

⚠️ `target: 'es2020'` и `cssCodeSplit: true` добавлять **не нужно** — это уже дефолты
Vite 5. Единственная осмысленная правка — vendor-чанк.

---

## P3 — доступность

### 18. Бегущую строку нельзя остановить с клавиатуры и с тача · high · M

[Marquee.tsx:12](../src/components/Marquee.tsx:12) — пауза только по `group-hover`.
Движение длится дольше 5 секунд и не имеет доступного механизма остановки — WCAG 2.2.2.

**Fix:** кнопка «пауза/воспроизведение» с `aria-pressed`, **вынесенная из бегущего трека**
(внутри она уедет вместе с ним). Вариант «продублировать на `focus-within`» — недостаточная
полумера: тач-пользователям она не помогает.

### 19. Тач-цели меньше минимума `[замерено]` · medium · S

**Замер** реальных боксов: пункты меню `work` 36×16, `team` 36×16, `process` 64×16,
`contact` 64×16; логотип `VE` 31×24; почта в футере 335×**17**.
Минимум WCAG 2.5.8 (AA) — 24×24 CSS-px; по высоте не проходит ни один из перечисленных.

**Fix:** вертикальные паддинги на ссылки (`py-2` даёт 32 px высоты) — визуально
ничего не сдвинет, так как шапка и футер уже flex-выровнены.

### 20. `mix-blend-difference` на шапке: контраст позиционный · medium · M

Над доминирующим `#0D0D0D` difference даёт ≈`#E7E7E3`, контраст ~15:1 — всё в порядке.
Провал возникает **точечно**, когда под шапкой светлый или акцентный контент — и это
происходит на первом экране (оранжевый CTA, крупный h1).

**Fix:** отслеживать пересечение шапки с акцентными блоками и переключать её
в обычный режим с явным фоном, либо дать шапке полупрозрачную подложку при `scrolled`.

### 21. Нет фирменных focus-стилей `[замерено]` · medium · S

**Замер:** UA-кольцо на месте — при реальном Tab на пункте меню computed style даёт
`outline: auto 1px rgb(229,151,0)`, `:focus-visible` матчится.
Tailwind preflight `outline` не обнуляет, поэтому **формально SC 2.4.7 не провален** —
первичная формулировка «блокер клавиатурной навигации» неверна.

Проблема в другом: кольцо не спроектировано и находится внутри
`mix-blend-difference`-шапки, где его цвет инвертируется непредсказуемо.

**Fix:** явные `focus-visible:` утилиты в фирменном акценте
(`focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent
focus-visible:outline-offset-2`) на всех ссылках и CTA.

### 22. `<footer>` внутри `<main>` · medium · S · ЧАСТИЧНО ЗАКРЫТО

Первая половина закрыта контент-релизом v2: секция Contact перестроена, её `h2` —
это «Есть задача — есть расчёт», крупнейший визуальный заголовок больше не `<p>`.

**Осталось:** `<footer>` по-прежнему вложен в `<main>` вместо уровня страницы.
**Fix:** вынести из [Contact.tsx](../src/components/Contact.tsx) в
[App.tsx](../src/App.tsx:15), реквизиты брать из того же `data/contact.ts`.

### 23. Ниже `md` навигации нет вовсе · medium · M

[Nav.tsx:35](../src/components/Nav.tsx:35) — `hidden … md:flex`. Бургера нет.
Контент не становится недостижимым (CTA `#contact` есть в Hero, остальное — скроллом),
но быстрые переходы к `#work`/`#process`/`#team` на мобильном теряются, и единственная
кнопка в мобильной шапке уводит на внешний `t.me`.

⚠️ «Просто показать 4 ссылки в строку» на 375 px не поместится вместе с логотипом
и кнопкой Telegram в одном flex-ряду — нужен либо бургер, либо перенос.

### 24. Английские вставки не размечены `lang="en"` · low · S ✅ ЗАКРЫТО

`lang="en"` проставлен микро-лейблу Hero, ◆-надстрочникам секций, пунктам меню
и подписям Telegram/Email/GitHub. Роли команды переведены на русский
(«Креативный продюсер», «Технический директор») — там вопрос снялся сам.

### 25. Клон списка в marquee читается скринридером дважды · low · S

Дублирование в [Marquee.tsx:5](../src/components/Marquee.tsx:5) нужно для бесшовной
петли. Это избыточность озвучки, а не барьер.

**Fix (минимальный):** `aria-hidden` на весь трек + визуально скрытый список имён
рядом. Расщеплять рендер на два трека не обязательно.

### 26. Скроллбар принудительно скрыт · low · S

[index.css:39-48](../src/index.css:39). Формально WCAG не нарушен, но пользователь
теряет индикатор позиции и возможность тащить полосу мышью на странице высотой ~7000 px.

**Fix, сохраняющий дизайн-намерение:** не убирать блок, а заменить на
`scrollbar-width: thin` + `scrollbar-color` под тёмную тему.

### 27. Контурный текст `color: transparent` · low · M

`.text-hollow-white` — обводка `rgba(244,244,240,0.35)`, контраст ≈3.0:1.
Весь текст этого класса — крупный (порог AA для крупного = 3:1), то есть это **граница,
а не провал**. Аргумент «обводка читается хуже заливки» верен, но WCAG его не измеряет.

**Fix:** поднять альфу обводки до ~0.5 в marquee. Приоритет низкий.

---

## P4 — код и дизайн-система

Ничего из этого не видит пользователь. Брать, когда нужен «подмести за собой» коммит.

| # | Что | Где | Severity |
|---|-----|-----|----------|
| 28 | ✅ **ЗАКРЫТО** контент-релизом v2: в компонентах не осталось пользовательских строк | — | — |
| 29 | Константа `EASE` продублирована в 7 TS-файлах, `cubic-bezier` — ещё 5 раз в CSS | все компоненты, [index.css](../src/index.css) | low |
| 30 | Класс `.hairline` мёртв **и неработоспособен**: preflight обнуляет `border-width`, поэтому один `class="hairline"` вообще ничего не нарисует | [index.css:86](../src/index.css:86) | low |
| 31 | Токен `surface: #1A1A1A` не используется нигде | [tailwind.config.js:8](../tailwind.config.js:8) | low |
| 32 | Шапка секции скопирована дословно в 3 компонентах | Cases/Process/Team | low |
| 33 | `.mono-label` существует, но подписи набраны вручную — 5 разных значений `tracking`. Сам токен рассинхронизирован: в нём `0.18em`, в разметке чаще `0.16em`/`0.14em` | [index.css:91](../src/index.css:91) | low |
| 34 | ✅ **ЗАКРЫТО**: в коде `md:odd:`/`md:even:`, третий участник бордеры не сломает | — | — |
| 35 | `RevealText` рендерит `<div>` внутри `<span>` при дефолтном `as` — формально невалидный HTML. Практических последствий нет (CSR без гидрации, оба `inline-block`) | [RevealText.tsx:26](../src/components/ui/RevealText.tsx:26) | low |
| 36 | Акцент и базовые цвета продублированы сырыми хексами в CSS. Значения сегодня идентичны — это риск расхождения при ребрендинге, а не баг | [index.css:7,36,104,111](../src/index.css:7) | low |
| 37 | Метрики карточек в жёстком `grid-cols-3`; тесно на узких экранах. **Перемерено 2026-08-06:** в модалке на 375 px «>35 млн ₽» даёт 90 px ink в 88-px боксе — выходит в гэп, но не обрезается и не переносится | [CaseCard.tsx:70](../src/components/ui/CaseCard.tsx:70), [CaseModal.tsx:234](../src/components/ui/CaseModal.tsx:234) | low |

---

## P5 — инфраструктура

| # | Что | Severity | Комментарий |
|---|-----|----------|-------------|
| 38 | Нет CI: `.github` отсутствует, build и typecheck не проверяются автоматически | medium | Build падает громко и до выкладки, так что «битый прод» не возникает — теряется время на релизе |
| 39 | Нет аналитики: доля доскроллов и кликов по Telegram неизмерима | medium ❓ | Форма/бэкенд — **сознательное продуктовое решение**, не трогать. Пробел — только измерение |
| 40 | Целевая платформа деплоя нигде не зафиксирована | medium ❓ | `base` в `vite.config.ts` менять **не надо** — дефолт `"/"` верен для Vercel/Netlify/своего домена |
| 41 | Нет тестов и смоук-скрипта | low | Дешёвый минимум: проверка, что рендерятся 5 секций с `id` (`top`, `work`, `process`, `team`, `contact` — у Marquee и USP их нет) |
| 42 | Нет `.gitattributes` | low | `* text=auto eol=lf` — единственное, что защищает от уже существующего риска CRLF-шума на Windows |
| 43 | Версия Node не зафиксирована | low | Lock-файл есть, поэтому «разный dist» не воспроизводится; риск — падение `npm ci` на старом Node |
| 44 | Нет Prettier / `.editorconfig` | low | Окупается, когда над репо начнёт работать вторая машина |
| 45 | `include` в tsconfig не покрывает `tailwind.config.js` | low | В файле уже есть `/** @type {...Config} */`, в редакторе типизация работает |
| 46 | Зависимости диапазонами `^` без Dependabot | low | ⚠️ `npm install` при консистентном lock **не поднимает** версии — дрейфа без `npm update` не будет |

---

## ❓ Вопросы владельцу продукта

Не технические решения — их нельзя принять из кода.

### Отвечено 2026-08-05

| Вопрос | Решение владельца |
|---|---|
| `M.VIDEO` / `M.ВИДЕО` — приём или опечатка? | Опечатка, привести к `М.Видео`. ✅ Сделано. Дубль остался — см. блок «Закрыто» |
| Дубль h2 «Инженер × Продюсер» | Убрать, в Team другой заголовок. ✅ Сделано — «Кто строит» |
| Прод-домен | `vibeengineering.ru`. ✅ Проставлен в `canonical`, `og:url`, `og:image` |
| OG-картинка | Генерировать динамически, политику «ноль ассетов» не нарушать. ✅ Сделано на Satori в build-time |

> По OG: из двух предложенных вариантов взят **Satori**, а не `@vercel/og`.
> Причина: `@vercel/og` — edge-функция, ей нужен рантайм Vercel, а платформа деплоя
> в проекте не зафиксирована (§40) и серверной части нет вовсе. Карточка
> одностраничника не варьируется, поэтому build-time даёт тот же результат
> и работает на любом статическом хостинге. Если Vercel всё-таки будет выбран
> и понадобятся динамические параметры — переезд сведётся к переносу той же
> разметки в edge-роут.

### Отвечено контент-релизом v2 (2026-08-05)

| Вопрос | Решение владельца |
|---|---|
| Дубль М.Видео в ленте | Снят: лента приведена к копирайту — `Kupikod · Альфа-Банк · М.Видео · Poison Drop · Сколково · VK Fest · ВДНХ` |
| JSON-LD, `robots.txt`, `sitemap.xml` | Делать. ✅ Сделано |
| Метрики Hero | `48` часов до первого запуска · `400+` реализованных проектов · `30M+` охватов в кампаниях |
| Порядок кейсов | Воронка доверия, а не хронология: М.Видео → VEgroove → Kupikod → Альфа-Банк |
| Мат в финальном оффере | Заменить на «Проекты начинаются с одного сообщения.» ✅ Сделано |
| Медиа в репозитории | Подключить архив к кейсам. Политика уточнена: производные в git, оригиналы — нет |

### Открыто

1. **Кириллица в брендовых шрифтах (§0)** — самый крупный открытый вопрос,
   требует выбора гарнитуры. Решение бренда, не разработки. Контент-релиз v2 сделал
   его заметнее: в ленте клиентов стало больше кириллицы (см. §49).
2. **ИНН и ОГРН для футера** (§47) — поле готово, значения нужны от владельца.
3. **Противоречие в постановке процесса.** ТЗ дало две несовпадающие версии этапов;
   взят более поздний копирайт, но длительность этапа 02 выставлена в «48 часов»,
   потому что владелец отдельно потребовал убрать расхождение с обещанием Hero.
   Если задумано было иначе — правится одной строкой в
   [data/process.ts](../src/data/process.ts).

---

## Опровергнутые находки

Зафиксированы, чтобы их не «нашли» заново. Каждая проверена по коду или замером.

| Находка | Почему неверна |
|---------|----------------|
| Переполнение строк на 320 px | **Замерено:** при `clientWidth = 320` ни один элемент не выходит за вьюпорт (`unclippedCount: 0`), ink-overflow у меток Contact = 0, горизонтального скролла нет (`window.scrollX` остаётся 0). Часть про футер неверна отдельно: ниже `md` он `flex-direction: column` |
| `.gitignore` не закрывает `.env` | Это стоковый шаблон Vite, где `.env` предназначен для коммита, а секреты идут в `.env.local` (покрыт `*.local`). Любая `VITE_*`-переменная по построению попадает в клиентский бандл и секретом быть не может |
| Имя `process` в `data/process.ts` затеняет глобаль Node | Импорт создаёт модульную привязку только внутри файла. Предложенный фикс `"types": ["vite/client"]` **сломал бы сборку**: tsconfig включает `vite.config.ts`, который использует `node:path` и `__dirname` |
| Индикатор прогресса ленты стартует с неверной шириной (18.3 % против верных 71.4 %) | **Артефакт dev-сервера, не прода:** в dev Tailwind приезжает инъекцией через JS, и первый `measure()` успевает до применения стилей. В проде CSS — render-blocking `<link>`, к монтированию вёрстка уже верная. Значение само исправляется на первом `scroll`/`resize` |
| `LazyVideo` никогда не грузит ролик | **Артефакт панели браузера:** IntersectionObserver там не доставляет колбэки вообще — проверено собственным наблюдателем на `h1` первого экрана. Тем же объясняются несработавшие lazy-картинки, `whileInView` и smooth scroll. Замер веса в `REPORT-cases-rail.md` (1246 kB по клику) доказывает, что путь рабочий. Подробности — [отчёт §5](REPORT-multi-review-2026-08-06.md) |
| Загружаются лишние начертания 300/700 | **Замерено:** объявлен 31 `@font-face`, загружено 6. Браузер качает woff2 только для применённых начертаний; неиспользуемые стоят десятки байт в CSS |
| `transition-all` на padding шапки — layout-анимация | Шапка `position: fixed` (вне потока), поддерево из 4 узлов, переход отрабатывает один раз при пересечении порога 24 px. Предложенный `scale-y-[0.85]` сжал бы логотип — фикс хуже проблемы |
| Типы не фиксируют число метрик | Во всех данных ровно по 3 элемента. Профилактическое ужесточение, а не дефект. Реально хрупкое место другое — вёрстка Team на 2 карточки (п.34) |
| Нет `scroll-padding-top` под фиксированной шапкой | Секции имеют `py-24`/`md:py-40` — заголовок не попадает под шапку высотой 56–72 px |
| Marquee: hover-переход обводки перерисовывает большой слой на ходу | Наведение сначала ставит ленту на паузу (`group-hover:[animation-play-state:paused]`), переход отрабатывает на неподвижном слое |
| vw-типографика без `clamp()` вызывает CLS | Пересчёт происходит только при resize окна; у пользователя с фиксированным вьюпортом не проявляется |
